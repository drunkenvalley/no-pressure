import { BlockStyleDefinition } from "sanity";
import StrongText from "@/components/Text/Strong";

const StrongTextStyle: BlockStyleDefinition = {
  title: "Shiny",
  value: "shiny",
  component: StrongText,
};

export default StrongTextStyle;
