import { BlockStyleDefinition } from "sanity";
import ShinyText from "@/components/Text/Shinytext";

const ShinyTextStyle: BlockStyleDefinition = {
  title: "Shiny",
  value: "shiny",
  component: ShinyText,
};

export default ShinyTextStyle;
