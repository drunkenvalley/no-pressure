import { BlockStyleDefinition } from "sanity";
import SmallText from "@/components/Text/Small";

const SmallTextStyle: BlockStyleDefinition = {
  title: "Small",
  value: "small",
  component: SmallText,
};

export default SmallTextStyle;
